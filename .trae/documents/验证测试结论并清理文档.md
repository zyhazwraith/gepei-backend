# UI/UX 优化实施计划

根据 `FP01-FP05_TEST_REPORT.md` 中的改进建议，我将对前端注册和地陪认证页面进行以下优化：

## 1. 注册页面优化 (`client/src/pages/Register.tsx`)
- **密码可见性**:
    - 为"设置密码"和"确认密码"输入框添加"显示/隐藏"切换按钮（复用登录页的 `Eye/EyeOff` 图标逻辑）。
    - 提升移动端输入的容错率。
- **协议链接**:
    - 将"用户协议"和"隐私政策"文字替换为可点击的链接（暂时指向 `#`，后续可替换为真实路由），增强交互感。

## 2. 地陪资料编辑优化 (`client/src/pages/GuideEdit.tsx`)
- **身份证号实时校验**:
    - 添加 `onBlur` (失去焦点) 事件监听。
    - 当用户输入完移开光标时，立即检查身份证号格式（18位）。
    - 如果格式错误，在输入框下方显示红色错误提示，而不是等到点击保存才报错。

## 3. 验证
- 修改完成后，前端页面将自动热更新。
- 我将确认代码无语法错误。

确认执行这些 UI 修改吗？