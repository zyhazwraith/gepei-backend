# 文档清理计划

我已仔细检查了 `docs/` 和 `.trae/documents/` 目录。

## 1. 建议清理 (Delete)

`.trae/documents/` 目录下的所有文件均为 Agent 自动生成的临时计划文档（如"MySQL 内存优化计划"、"FP-005 实施计划"等）。这些是过程性记录，任务完成后已无保留必要。

**操作**: 删除 `.trae/documents/` 目录及其下所有文件。

## 2. 建议保留 (Keep)

### `docs/` 目录下的文档均为正式的项目资产，必须保留：

### **核心设计**: 架构/数据库/API/任务清单 (V1.1 - V1.3)。

### **业务流程**: `USER_FLOW_V2.md` (非常详细，包含完整的定制单和后台管理流程，是后续开发的重要参考)。

### **交付记录**: `qa_handovers/` 下的转测报告。

### 确认执行清理吗？
