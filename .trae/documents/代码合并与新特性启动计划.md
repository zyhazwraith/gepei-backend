# 合并与新特性开发计划

你确认要合入 `master` (应为 `main`)，推送到远程，并开始下一个 Feature 开发。

## 1. 代码合并与推送
我们将把当前的 `feature/trae-setup-fixes` 合并回 `main` 分支，并同步到 GitHub。

- **切换分支**: `git checkout main`
- **合并代码**: `git merge feature/trae-setup-fixes`
- **推送到远程**: `git push origin main`

## 2. 下一个特性规划 (Feature Planning)
根据项目文档和进度，下一个重要的功能点是 **FP-005 (地陪接单与订单管理)** 或 **FP-006 (支付集成)**。
但查看之前的文档 `docs/04_功能点开发任务_V1.1.md`，FP-005 涉及"地陪展示与搜索"的完善。

建议下一个 Feature 为：**`feature/FP-005-guide-discovery`** (地陪展示与搜索)。
- **目标**: 让用户能在首页看到地陪列表，点击进入详情页，并能按城市/标签筛选。
- **涉及文件**: 后端 API (`GET /guides`), 前端页面 (`Guides.tsx`, `Home.tsx`).

## 执行步骤
1.  执行 Git 合并与推送操作。
2.  创建新分支 `feature/FP-005-guide-discovery`。
3.  更新 Todo 列表，准备开始 FP-005 开发。

确认执行吗？