我将按照 `docs/qa_handovers/FP005_HANDOVER.md` 中的建议，扩展测试范围，确保地陪认证与展示功能的健壮性。

### 1. 扩展自动化测试 (Expand Automation)
**目标**: 更新 `scripts/test-api-flow.ts`，增加对地陪认证后数据一致性和展示逻辑的验证。
**新增测试点**:
- **地陪资料回显**: 认证成功后，调用 `GET /guides/profile` 确认信息正确返回。
- **地陪列表展示 (Search)**:
  - 验证新认证的地陪是否出现在 `GET /guides` 列表中。
  - 验证按城市 (`city=Beijing`) 筛选功能。
  - 验证按关键词 (`keyword=Intro`) 搜索功能。
  - 验证隐私保护 (列表接口不应返回身份证号)。

### 2. 执行与验证 (Execute & Verify)
- 运行更新后的测试脚本 `npm run test:api`。
- 确认所有新增测试用例通过。

### 3. 更新转测报告 (Update Handover Doc)
- 将最新的测试结果（特别是列表搜索和隐私保护的验证结果）补充到 `docs/qa_handovers/FP005_HANDOVER.md` 的 "自测结果" 章节，保持文档与实际测试进度一致。

**本次执行**: 优先升级测试脚本，覆盖列表搜索与隐私保护场景，然后运行测试并更新文档。
