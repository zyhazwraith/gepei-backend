# 代码合并任务

好的，我们先专注于完成代码合并。

## 执行步骤
1.  **提交更改**: 将新增的 `scripts/seed-data.ts` 和 `package.json` 变更提交到当前分支。
2.  **切换分支**: 切换回 `main` 分支。
3.  **合并分支**: 将 `feature/FP-005-guide-discovery` 合并入 `main`。
4.  **推送远程**: 将最新的 `main` 推送到 GitHub。

合并完成后，我会通知你，届时再详细讨论后续任务。

确认执行吗？